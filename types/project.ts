export default interface IProject{
    name: string
    desc: string
    tags: string[]
    primaryImage: string
    secondaryImage: string
    techSheet: string[]
    availability: string
    credits: string[]
    id: string
}